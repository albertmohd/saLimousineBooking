{
  "buildCommand": "vite build --root client --outDir ../dist/public --emptyOutDir",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
