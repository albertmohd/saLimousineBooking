{
  "buildCommand": "cd client && vite build --outDir ../dist/public --emptyOutDir",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
